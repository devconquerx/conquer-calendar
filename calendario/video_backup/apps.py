from django.apps import AppConfig


class VideoBackupConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'calendario.video_backup'
    verbose_name = 'Respaldo de vídeo'
